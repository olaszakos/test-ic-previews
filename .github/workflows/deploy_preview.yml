name: Deploy Preview
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  request_preview_canister:
    runs-on: ubuntu-latest

    steps:
      - run: |
          curl -v https://api.coinbase.com/v2/prices/ICP-USD/buy
