name: Deploy Preview
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  request_preview_canister:
    runs-on: ubuntu-latest

    steps:
      - run: |
          curl -v https://api.binance.com/api/v3/ticker/24hr\?symbol\=ICPUSDT
